# API Backend Server URL
# The frontend now proxies all API calls through the backend server
VITE_API_URL=http://localhost:3001

# Supabase Auth Configuration (for Google OAuth)
# These are safe to expose in the frontend - they're public keys
# Get these from Supabase Dashboard > Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: All SECRET API keys (Supabase Service Role, Vapi)
# are stored securely on the backend server in /server/.env
# See /server/.env.example for the required server configuration
